<div class="kt-world-map-wrapper">
  <div class="kt-map-svg-layer">
    {% comment %} 這裡引入您原本的 icon-world-map.liquid {% endcomment %}
    {% render 'icon-world-map' %}
  </div>

  <div class="kt-landmarks-layer">
    
    {% for block in section.blocks %}
      {% assign tooltip_class = '' %}
      {% assign pos_y = block.settings.pos_y | plus: 0 %}
      {% if pos_y < 45 %}
        {% assign tooltip_class = 'kt-tooltip-bottom' %}
      {% endif %}
      <div class="kt-hotspot {{ tooltip_class }}" style="top: {{ block.settings.pos_y }}%; left: {{ block.settings.pos_x }}%;" {{ block.shopify_attributes }}>
        
        <div class="kt-pin-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7z" fill="#E31D1A"/>
            <circle cx="12" cy="9" r="2.5" fill="white"/>
          </svg>
          <div class="kt-pin-pulse"></div>
        </div>

        <div class="kt-artisan-card">
          <div class="kt-card-inner">
            <div class="kt-card-image">
              {% if block.settings.image != blank %}
                {{ block.settings.image | image_url: width: 240 | image_tag: loading: 'lazy', alt: block.settings.name }}
              {% else %}
                <img src="https://placehold.co/80x80/png?text=PIC" alt="Artisan">
              {% endif %}
            </div>
            <div class="kt-card-info">
              {% if block.settings.country != blank %}
                <div class="kt-artisan-country">{{ block.settings.country }}</div>
              {% endif %}
              <h4 class="kt-artisan-name">{{ block.settings.name }}</h4>
              <p class="kt-artisan-desc">{{ block.settings.description }}</p>
              <a href="{{ block.settings.link | default: '#' }}" class="kt-more-btn">MORE</a>
            </div>
          </div>
        </div>
        
      </div>
    {% endfor %}

  </div>
</div>

<style>
  /* 容器設定：確保地標層與地圖層完美重疊 */
  .kt-world-map-wrapper {
    position: relative;
    width: 100%;
    max-width: 1440px; /* 依據您的版面調整 */
    margin: 0 auto;
    {% comment %} background-color: #f4f4f4; /* 示意背景色 */ {% endcomment %}
    overflow: visible; /* 允許小卡超出邊界 */
  }

  /* 1. 地圖層：讓 SVG 自適應寬度 */
  .kt-map-svg-layer svg {
    width: 100%;
    height: auto;
    display: block;
    fill: #d1d1d1; /* 預設地圖顏色 */
  }

  /* 2. 地標層：覆蓋在上方 */
  .kt-landmarks-layer {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none; /* 讓點擊穿透到地圖，除非點到地標 */
  }

  /* 熱點容器 */
  .kt-hotspot {
    position: absolute;
    transform: translate(-50%, -100%); /* 讓定位點對準 Icon 底部尖端 */
    pointer-events: auto; /* 恢復互動 */
    cursor: pointer;
    {% comment %} z-index: 10; {% endcomment %}
     z-index: 9999; /* 強制置頂 */
  }

  /* A. 紅色地標樣式 */
  .kt-pin-icon {
    position: relative;
    transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    filter: drop-shadow(0 2px 4px rgba(0,0,0,0.2));
  }
  
  /* Hover 時地標放大 */
  .kt-hotspot:hover .kt-pin-icon {
    transform: scale(1.2) translateY(-5px);
    z-index: 20;
  }

  /* 呼吸燈動畫 (Optional) */
  .kt-pin-pulse {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
    height: 100%;
    background: rgba(227, 29, 26, 0.4);
    border-radius: 50%;
    z-index: -1;
    animation: pulse 2s infinite;
  }
  
  @keyframes pulse {
    0% { transform: translate(-50%, -50%) scale(0.5); opacity: 1; }
    100% { transform: translate(-50%, -50%) scale(2); opacity: 0; }
  }

  /* B. 職人小卡樣式 (依據 image_3ee674.png 設計) */
  .kt-artisan-card {
    position: absolute;
    bottom: 100%; /* 出現在地標上方 */
    left: 50%;
    transform: translateX(-50%) translateY(10px);
    width: 200px;
    background: white;
    padding: 10px;
    border-radius: 8px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.15);
    border: 1px solid #eee;
    
    /* 隱藏狀態 */
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    pointer-events: none;
    z-index: 100;
    margin-bottom: 12px; /* 與地標的距離 */
  }

  /* Hover 觸發顯示小卡 */
  .kt-hotspot:hover .kt-artisan-card {
    opacity: 1;
    visibility: visible;
    transform: translateX(-50%) translateY(0);
    pointer-events: auto;
  }

  /* 小卡箭頭 */
  .kt-artisan-card::after {
    content: '';
    position: absolute;
    bottom: -6px;
    left: 50%;
    transform: translateX(-50%);
    border-width: 6px 6px 0;
    border-style: solid;
    border-color: white transparent transparent transparent;
  }

  /* 若地標位置太靠上 (Y < 40%)，則將小卡顯示在地標下方 */
  .kt-hotspot.kt-tooltip-bottom .kt-artisan-card {
    bottom: auto;
    top: 100%;
    margin-bottom: 0;
    margin-top: 12px;
    transform: translateX(-50%) translateY(-10px);
  }

  /* 下方顯示時的箭頭方向調整 */
  .kt-hotspot.kt-tooltip-bottom .kt-artisan-card::after {
    bottom: auto;
    top: -6px;
    border-width: 0 6px 6px;
    border-color: transparent transparent white transparent;
  }

  /* 修正下方顯示時的 Hover 動畫 (解決權重問題) */
  .kt-hotspot.kt-tooltip-bottom:hover .kt-artisan-card {
    transform: translateX(-50%) translateY(0);
  }

  /* 小卡內部排版 */
  .kt-card-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .kt-card-image img {
    width: 100%;
    max-width: 120px;
    height: 120px;
    object-fit: cover;
    border-radius: 4px;
    margin-bottom: 8px;
    border: 1px solid #ddd;
  }

  .kt-artisan-country {
    font-size: 12px;
    color: #E31D1A; /* 使用品牌紅 */
    font-weight: bold;
    margin-bottom: 4px;
  }

  .kt-artisan-name {
    margin: 0;
    font-size: 14px;
    font-weight: 700;
    color: #333;
  }

  .kt-artisan-desc {
    margin: 2px 0 8px;
    font-size: 12px;
    color: #666;
  }

  .kt-more-btn {
    display: inline-block;
    padding: 5px 12px;
    background-color: #E31D1A; /* King Tony Red */
    color: white;
    text-decoration: none;
    font-size: 10px;
    font-weight: 600;
    border-radius: 20px;
    transition: all 0.3s ease;
  }

  .kt-more-btn:hover {
    background-color: #c21916;
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(227, 29, 26, 0.3);
  }

  
  /* 手機版 RWD 調整 */
  @media (max-width: 768px) {
    /* 1. 修正 Hotspot 定位方式：移除 transform 以便讓子元素的 fixed 定位生效 */
    .kt-hotspot {
      transform: none !important; /* 強制移除 transform */
      margin-left: -10px; /* 修正水平中心 (icon 寬 20px 的一半) */
      margin-top: -20px;  /* 修正垂直底部 (icon 高 20px) */
    }

    /* 2. 職人小卡：底部滑出樣式 */
    .kt-artisan-card {
      position: fixed; /* 固定在螢幕視窗 */
      left: 0;
      top: auto;
      bottom: 0;
      width: 100%;
      height: 220px; /* 固定高度 */
      margin: 0;
      border-radius: 20px 20px 0 0; /* 上方圓角 */
      background: white;
      z-index: 2147483647; /* 確保在最上層 */
      
      /* 動畫設定：從底部滑出 */
      transform: translateY(101%); 
      transition: transform 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
      
      /* 顯示設定 */
      opacity: 1;
      visibility: visible;
      box-shadow: 0 -4px 20px rgba(0,0,0,0.2);
      
      /* 內容排版 */
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* 3. 觸發顯示 (支援點擊與 Focus) */
    .kt-hotspot:hover .kt-artisan-card,
    .kt-hotspot:active .kt-artisan-card,
    .kt-hotspot:focus-within .kt-artisan-card {
      transform: translateY(0);
    }

    /* 4. 隱藏原本的小箭頭 */
    .kt-artisan-card::after {
      display: none;
    }

    /* 5. 內容佈局調整 (改為橫向排列) */
    .kt-card-inner {
      flex-direction: row;
      width: 100%;
      padding: 0 24px;
      gap: 20px;
      text-align: center;
    }

.kt-card-image {
      width: 50%;
      height: auto;
      background-color: red; /* 示意圖片區背景色 */
      {% comment %} flex: 0 0 50%; {% endcomment %}
    }

    .kt-card-image img {
      width: 100%;
      height: auto;
      {% comment %} aspect-ratio: 1/1; /* 強制固定為正方形 */ {% endcomment %}
      object-fit: fill;
      margin-bottom: 0;
    }

    .kt-card-info {
      flex: 1;
    }

    .kt-artisan-name {
      font-size: 18px;
      margin-bottom: 4px;
    }
    
    .kt-artisan-desc {
      font-size: 13px;
      margin-bottom: 12px;
    }
  }
</style>
{% schema %}
{
  "name": "World Artisan Map",
  "tag": "section",
  "class": "section-world-map",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "背景顏色",
      "default": "#f4f4f4"
    }
  ],
  "blocks": [
    {
      "type": "landmark",
      "name": "📍 地標紅點",
      "settings": [
        {
          "type": "header",
          "content": "位置設定"
        },
        {
          "type": "range",
          "id": "pos_x",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "label": "左右位置 (X)",
          "default": 50
        },
        {
          "type": "range",
          "id": "pos_y",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "label": "上下位置 (Y)",
          "default": 50
        },
        {
          "type": "header",
          "content": "內容設定"
        },
        {
          "type": "text",
          "id": "country",
          "label": "國家",
          "default": "Taiwan"
        },
        {
          "type": "text",
          "id": "name",
          "label": "職人名稱",
          "default": "職人姓名"
        },
        {
          "type": "text",
          "id": "description",
          "label": "描述",
          "default": "專業技能"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "大頭照"
        },
        {
          "type": "url",
          "id": "link",
          "label": "連結"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "World Artisan Map (預設)",
      "blocks": [
        {
          "type": "landmark",
          "settings": {
            "pos_x": 82,
            "pos_y": 42,
            "name": "台灣職人"
          }
        }
      ]
    }
  ]
}
{% endschema %}